# Payment Destination Options (choose one):

# Option A: Dynamic ATXP Destination (Recommended)
# Connection string from ATXP accounts service for dynamic payment resolution
# ATXP_CONNECTION_STRING=https://accounts.atxp.ai/?connection_token=your_connection_token_here

# Option B: Static Funding Destination
# Fixed wallet address and network for all payments
FUNDING_DESTINATION=your_wallet_address_here
FUNDING_NETWORK=base

# Optional: Port for the server (defaults to 3010)
PORT=3010

# Optional: ATXP authentication server (defaults to https://auth.atxp.ai)
ATXP_SERVER=https://auth.atxp.ai

# Optional: Set to development for local testing with HTTP
NODE_ENV=development